<div class="row mx-auto mb-3">
    <a class="btn btn-success" href="/notes/register">Adicionar Nota</a>
</div>
<div class="row">
    {{#each notes}}
    <div class="col-md-3 mb-2">
        <div class="card">
            <div class="card-body">
                <h3 class="card-title d-flex justify-content-between align-items-center">
                    {{titulo}}
                    <div class="dropdown show">
                        <a class="btn btn-primary dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
                            data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <i class="fas fa-cog"></i>
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuLink">
                            <a class="dropdown-item">
                                <form action="/notes/edit/{{_id}}" method="GET">
                                    <button type="submit" class="btn btn-primary btn-block btn-sm">Editar</button>
                                </form>
                            </a>
                            <a class="dropdown-item">
                                <form action="/notes/delete/{{_id}}?_method=DELETE" method="POST">
                                    <input type="hidden" name="_method" value="DELETE">
                                    <button type="submit" class="btn btn-danger btn-block btn-sm">Excluir</button>
                                </form>
                            </a>
                        </div>
                    </div>
                </h3>
                <hr>
                <p class="lead">{{descricao}}</p>
                <div class="d-flex justify-content-between align-items-center">
                    <div><span class="badge badge-success">4</span> Concluída(s)</div>
                    <div><span class="badge badge-warning">3</span> Pendente(s)</div>
                </div>
                <hr>
                <a href="/tasks/register/{{_id}}" class="btn btn-success btn-block">Acessar Tarefa(s)</a>
            </div>
        </div>
    </div>
    {{else}}
    <div class="col-md-3 mx-auto">
        <div class="card mx-auto">
            <div class="card-body text-center">
                <h3 class="card-title">Nenhuma nota encontrada.</h3>
                <a href="/notes/register" class="btn btn-success btn-block">Criar uma nota</a>
            </div>
        </div>
    </div>
    {{/each}}
</div>